# Session 07 Reflection

## Why Lists Beat Variables at Scale

Lists are better than variables because one list can store large amounts of elements meanwhile one variable can only store one element. 
One line of code can get multiple elements by looping through a list. Multiple lines of code would be needed if separate variables are used. 
This means that lists will reduce the amount of code needed and enhance maintainability.
